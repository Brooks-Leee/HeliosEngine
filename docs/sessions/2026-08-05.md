# 2026-08-05

## 00:59 — [agent:Claude] — 工具链清理：停用 superpowers 插件 + 移除 spec/plan 产物

### 完成
- 停用 superpowers 插件（`~/.claude/settings.json` → `enabledPlugins` 置 `false`）：去掉了每次会话开头强塞的 `using-superpowers` 注入，skill 列表下次会话起消失
- 删除项目内 superpowers 产物：`.superpowers/sdd/`（未跟踪的临时任务状态）+ `docs/superpowers/`（6 份 spec/plan，git 跟踪）→ 提交 `fe9e118`（已推送）
- 新增项目 README（简介 / 技术栈 / 构建 / 进度 / 笔记索引 / Agent 协作 / 编码规范）→ 提交 `30db64a`（已推送）
- 提交纪律确认：用户要求 commit 精简清晰——单一主题、短标题、不杂糅（已入长期记忆）

### 卡点
- 无

### 下一步
- 无（工具链清理收尾）

## 00:59 — [agent:Claude] — 笔记 1.9 重写 + STATUS 方向表述更新

### 完成
- 重写笔记 1.9：去掉项目历史叙事（之前/之后、生产版/最简版、时间戳提示），改为概念结构——路径 A（host-visible 直读，零拷贝但慢）/ 路径 B（staging + DMA 进显存）/ 决策表 / 移动端 vs 桌面端（UMA 反转结论）/ Vulkan vs DX12 对照
- 路径 A 补全：HOST_VISIBLE 在分配时选（memoryTypeIndex，不在 buffer 创建）、GPU 侧读取链路（bind → vertex fetch 按 vertexInput 布局取字节流）、工程提示（选型循环只跑一次缓存复用 + `memoryTypeBits` 过滤）
- 全篇缩写首次出现写全称（DMA/PCIe/UBO/VS/GDDR/SoC/UMA/DRAM/LPDDR/ASTC/ETC2/APU，共 12 个），索引行同步更新 → 提交 `d4e9dcc`
- 会话中确认的概念：内存类型标志（DEVICE_LOCAL / HOST_VISIBLE / HOST_COHERENT）、UMA 架构、各 API 表达内存模型的差异（Vulkan flags / DX12 heap / Metal storage mode / GL 隐藏）、UE RHI 层如何分离"资源创建 vs 上传"（RHIUpdateBuffer / Lock-Unlock / BUF_Static-Dynamic）
- STATUS.md 方向表述更新：去掉"暂停为 RHI 参考工具"，改为"与 Godot 项目并行推进、持续做引擎学习"（用户明确意图）

### 卡点
- 无

### 下一步
- depth attachment；Phase 1.3 DX12 Hello Triangle
