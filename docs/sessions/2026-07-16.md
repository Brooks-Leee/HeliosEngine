# 2026-07-16

## 14:30 — [agent:Claude] — Vertex Buffer 落地 + Vulkan 概念系统梳理

### 完成
- 把 base triangle 的硬编码顶点数组（位置/颜色）搬进真正的 GPU vertex buffer：
  - `triangle.vert` 改为 `layout(location=0) in vec2 inPos` / `location=1 in vec3 inColor`
  - C++ 加 `Vertex` 结构 + `g_TriangleVertices` 静态数据
  - 创建/上传：createBuffer → allocateMemory(host-visible+host-coherent) → bindBufferMemory → map/memcpy/unmap
  - pipeline 加 VertexInputBindingDescription + 2 个 VertexInputAttributeDescription，随管线编译
  - command buffer 录制时 `bindVertexBuffers(0,...)`；draw 逻辑与 push constant 不变
- 重新编译 triangle.vert.spv，build 通过，运行画面与之前一致（5 个反色三角形）
- 把本次对话打通的四个核心概念写成学习笔记 docs/notes/1.7–1.10：
  - 1.7 Render Pass / Subpass / Attachment
  - 1.8 Descriptor vs Push Constant
  - 1.9 Vertex Buffer 与上传
  - 1.10 Buffer 与 Memory 分配（三步分离 + sub-allocation 动机）
- 更新 docs/notes/README.md 索引（Phase 1 现 10 篇）

### 卡点
- 无。clangd 报的 Vulkan SDK 头文件找不到属环境问题，实际以 build.bat / CMake 为准，build 通过。

### 下一步
- MVP 矩阵：把 offset/scale 升级为真正的 model-view-projection uniform buffer（体会大参数走 descriptor 而非 push constant）
- depth attachment：加深度图让 5 个三角形有前后遮挡，亲手用上 attachment 的 depth/stencil 角色

### 备注（概念探索路径，用户驱动）
descriptor 是什么 → basepass 为何没 descriptor（走 push constant）→ subpass 必须 ≥1 → 一个 subpass 可否多 attachment → attachment 与 texture 区别 → attachment 与 VS 无关 → 加 depth + 真 vertex buffer（分两步，先 vertex buffer）。
