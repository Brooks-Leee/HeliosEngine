#include <windows.h>
#include <iostream>

int WINAPI WinMain(HINSTANCE hInstance,
                   HINSTANCE hPrevInstance,
                   LPSTR lpCmdLine,
                   int nCmdShow)
{
    std::cout << "HeliosEngine (ShuGuang) — Dawn is coming.\n";

    // Win32 window coming in Phase 1.
    // For now, just verify the build system works.

    return 0;
}
